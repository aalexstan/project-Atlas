# Source Monitoring

[Русская версия](SOURCE_MONITORING.ru.md)

Atlas uses a scheduled monitor to detect evidence that may need review. The monitor never changes API facts, prices, legal conclusions or `last_verified` dates.

## What Runs Automatically

- external URLs are discovered from active `apis/*/api.json` sources;
- availability, HTTP status and configured content markers are checked;
- API, comparison and need review dates are compared with the review cadence;
- an existing maintenance issue is updated, or one issue is created when attention is required.

## Source States

- `ok` - the source responded and configured markers matched;
- `restricted` - automated access received `401`, `403` or `429`; review may require a browser or provider access;
- `broken` - the source returned `404` or `410`;
- `unavailable` - timeout, network error, unexpected status or server failure;
- `changed` - a required marker disappeared or a configured fingerprint changed.

Restricted and changed sources are not automatically declared obsolete. A researcher must review the official source and update evidence before changing conclusions.

## Configuration

[`sources/source-registry.json`](../sources/source-registry.json) contains monitor defaults and optional per-URL markers or fingerprints. URLs do not need to be copied into the registry: active profile sources are discovered automatically.

Validate without network access:

```bash
python3 scripts/check_sources.py --validate-config
python3 -m unittest discover -s tests -p "test_*.py"
```

Run an explicit network check:

```bash
python3 scripts/check_sources.py --report source-status.md
python3 scripts/check_review_due.py --report review-due.md
```

Reports generated by scheduled runs are operational artifacts and are not committed automatically.

## Safety Boundary

Automation may open a review task. It must not:

- change a price because visible text changed;
- infer API rights from technical accessibility;
- update verification dates;
- claim live testing;
- accept marketing claims as independent evidence;
- publish credentials, response payloads or personal data.
