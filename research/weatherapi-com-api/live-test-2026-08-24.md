# WeatherAPI.com Live Test - 2026-08-24

[Russian version](live-test-2026-08-24.ru.md)

## Purpose

Verify a bounded set of WeatherAPI.com request and response claims with an authorized user-provided free-tier key. This is empirical evidence for the tested request shapes, not a quota, SLA, accuracy or legal benchmark.

## Authorization and safety

- The key was supplied by the user and used once for each request below.
- The key value is not recorded in this repository, logs or raw payloads.
- No paid credential, production secret or personal data was used.
- Five safe HTTP GET requests were made; no load test and no quota exhaustion was attempted.
- The provider documentation and [API terms](https://www.weatherapi.com/terms.aspx) were reviewed before testing. This record does not independently establish the user's account plan or contractual rights.

## Pre-test core claims

This list was frozen before the valid-key request series.

| ID | Core claim | Profile reference | Why core |
|---|---|---|---|
| C1 | WeatherAPI.com provides current weather, forecast and location search/autocomplete through documented API methods. | `api.json` `best_for`, `sources`; [official docs](https://www.weatherapi.com/docs/) | Identity and primary purpose |
| C2 | A valid API key and `q` parameter produce JSON responses for current weather, forecast and search. | `api.json` `authentication`; [official docs](https://www.weatherapi.com/docs/) | Primary response contract |
| C3 | Forecast accepts a `days` parameter and returns a multi-day forecast response. | `api.json` `best_for`, `open_questions`; [official docs](https://www.weatherapi.com/docs/) | Primary forecast capability |
| C4 | Free-tier quota, rate limits, pricing, SLA and data-rights boundaries are procurement claims that must remain provider-reported or unknown unless separately confirmed. | `api.json` `pricing`, `rate_limits`, `open_questions` | Commercial and limits block |
| C5 | Missing or ambiguous input produces a predictable, actionable result rather than silently selecting an unrelated location. | `api.json` `open_questions` | Integration safety claim |

## Test environment

- Date: 2026-08-24
- Client: `curl --silent --show-error --max-time 25`
- Location: `Moscow`
- Raw payloads: [live-test-2026-08-24-raw](live-test-2026-08-24-raw/)
- Rate-limit headers: none of the captured responses contained `Retry-After` or a documented quota header.

## Requests and observations

### 1. Current weather with field selection

`GET https://api.weatherapi.com/v1/current.json?key=<redacted>&q=Moscow&current_fields=temp_c,wind_kph`

Observed: HTTP `200`, `0.258207 s`, matched location `Moscow`, Russia. The response included `temp_c` and `wind_kph`, but also returned many other current-weather fields. The documented field-selection parameter therefore requires a separate plan/endpoint clarification; it was not treated as confirmed filtering behavior.

Raw payload: [current.json](live-test-2026-08-24-raw/current.json)

#### Follow-up field-filtering check

The official documentation specifies comma-separated `current_fields` and links to an [API response fields](https://www.weatherapi.com/my/fields.aspx) page. That page redirects to login without an authenticated session. Four one-request variants were checked with the same authorized key:

| Query variant | HTTP | Time | Observed current-field count | Result |
|---|---:|---:|---:|---|
| `current_fields=temp_c` | 200 | 0.455877 s | 39 | No visible filtering |
| `current_fields=temp_c,wind_kph` | 200 | 0.303526 s | 39 | No visible filtering |
| `fields=temp_c` | 200 | 0.349947 s | 39 | No visible filtering |
| `current_fields[]=temp_c` | 200 | 0.305538 s | 39 | No visible filtering |

This does not establish whether the feature is gated by plan/account, requires an authenticated fields configuration, or is stale/incorrect in the public documentation. Keep this as an open finding; do not label it an explained conflict.

### 2. Three-day forecast

`GET https://api.weatherapi.com/v1/forecast.json?key=<redacted>&q=Moscow&days=3&aqi=no&alerts=no`

Observed: HTTP `200`, `0.683815 s`, matched location `Moscow`, and `3` forecast days. The response was JSON with `location`, `current` and `forecast.forecastday` objects.

Raw payload: [forecast.json](live-test-2026-08-24-raw/forecast.json)

### 3. Search/autocomplete

`GET https://api.weatherapi.com/v1/search.json?key=<redacted>&q=Moscow`

Observed: HTTP `200`, `0.180209 s`, two results. Each observed result contained `id`, `name`, `region`, `country`, `lat`, `lon` and `url`.

Raw payload: [search.json](live-test-2026-08-24-raw/search.json)

### 4. Unknown-looking location string

`GET https://api.weatherapi.com/v1/current.json?key=<redacted>&q=atlas-invalid-location-zzzz`

Observed: HTTP `200`, `0.323943 s`, but the service resolved the string to `Atlas`, Eskisehir, Turkey. This is a material integration finding: a client must validate or review the matched location and must not equate HTTP 200 with an exact query match.

Raw payload: [unknown-location.json](live-test-2026-08-24-raw/unknown-location.json)

### 5. Missing required query parameter

`GET https://api.weatherapi.com/v1/current.json?key=<redacted>&q=`

Observed: HTTP `400`, `0.288702 s`, JSON error code `1003`, message `Parameter q is missing.`

Raw payload: [empty-q.json](live-test-2026-08-24-raw/empty-q.json)

## Rate-limit observation

The five-request sequence did not receive HTTP `429`, `Retry-After` or another rate-limit response. This does not measure the daily quota, burst limit or plan-specific rate limit. Those remain provider-reported or unknown.

## Claim comparison and findings

| Claim | Result | Status |
|---|---|---|
| Current, forecast and search methods exist and are reachable with a valid key | All three returned HTTP 200 JSON | Confirmed for tested shapes |
| Valid key plus `q` produces JSON | Confirmed for valid requests | Confirmed for tested shapes |
| `days=3` produces a three-day forecast | Three forecast days observed | Confirmed for tested request |
| `current_fields` restricts the response to selected fields | Four syntax variants returned all 39 current fields | Open finding; plan/account/docs behavior unresolved |
| Free-text search input is rejected or clearly marked when it is not an exact match | Input resolved to `Atlas`, Turkey with HTTP 200 | Open accuracy finding; unvalidated free text can silently resolve to an unrelated location |
| Missing `q` gives structured error | HTTP 400, code 1003 | Confirmed |
| Quota, rate limits, pricing, SLA and data rights | Not measurable in this safe test | Unknown/provider-reported |

## Decision

The test confirms bounded technical access, JSON responses, current/forecast/search request shapes and a structured missing-parameter error. It also found two open integration findings: field selection did not visibly narrow the response even across four syntax variants, and unvalidated free-text input can silently resolve to an unrelated location with HTTP 200. The first finding requires provider/account clarification; the second requires caller-side exact-match or confidence handling before downstream use.

Keep maturity at `reviewed`. Set `live_tested: true` only for this evidence window; do not promote to `verified`. The evidence is valid until `2026-11-22` under the shortest 90-day access/limits cadence, unless a material provider change requires earlier review.

See the [procedural pre-merge review](../../reviews/weatherapi-com-live-test-2026-08-24.md).

## Reproduction

Repeat the five redacted request shapes with a currently authorized key. Preserve response bodies, HTTP status, total latency and rate-limit headers. Do not print or commit the key, do not exhaust quotas, and inspect matched location fields before accepting a result.
